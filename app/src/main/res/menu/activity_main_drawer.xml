<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:id="@+id/nav_head"
           android:checkableBehavior="single">

        <!--
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/ic_menu_camera"
            android:title="Ausgaben" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Loeschen" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Todos" /> -->
    </group>

    <item android:title="Menü">
        <menu>
            <!--<item
                android:id="@+id/nav_ausgaben"
                android:icon="@drawable/ic_menu_manage"
                android:title="Ausgaben" />-->
            <item
                android:id="@+id/nav_tabellen"
                android:icon="@drawable/ic_menu_share"
                android:title="Tabellen" />
            <item
                android:id="@+id/nav_einstellungen"
                android:icon="@drawable/ic_menu_send"
                android:title="Einstellungen" />
        </menu>
    </item>

</menu>
